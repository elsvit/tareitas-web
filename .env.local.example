# Optional personal overrides (gitignored).
# Loaded in all dev modes and overrides .env.development / .env.prodapi.
#
# Example: force local API even when running yarn dev-prod
# VITE_API_URL=http://localhost:3000
